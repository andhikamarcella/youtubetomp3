YTConv 1.6.5 publication marker.

Native packages and the npm release are gated by exact-commit CI, registry byte verification, consolidated SHA-256 checksums, and a CycloneDX SBOM.

Authenticated npm publication retry after the exact package matrix and release assets passed verification.
