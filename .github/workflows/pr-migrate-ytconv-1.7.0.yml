name: Archived YTConv 1.7.0 iSH Synchronization

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The YTConv 1.7.0 iSH identity and checksum synchronization is complete."
