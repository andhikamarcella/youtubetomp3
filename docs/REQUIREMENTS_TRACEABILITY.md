# Requirements Traceability Matrix

Date: 2026-07-02 UTC.

Status values:
- `Implemented`: repository code and tests/checks exist.
- `Partial`: repository-side implementation exists, but more coverage or external configuration is needed.
- `Blocked-external`: requires credentials, production services, paid infrastructure, or browser/device lab access; repository adapters/docs/tests are still expected.
- `Not started`: no meaningful repository implementation yet.

| Phase / Batch | Requirement | Status | Affected files | Tests / checks | Blocker |
| --- | --- | --- | --- | --- | --- |
| Phase 0 | Repository inventory and baseline | Implemented | `docs/AUDIT_REPORT.md`, `package.json`, `scripts/lint.mjs`, `scripts/typecheck.mjs` | `npm install --ignore-scripts --no-audit --no-fund`, `npm test`, `npm run build`, `npm run lint`, `npm run typecheck` | None. |
| Phase 0 | Working docs and implementation plan | Implemented | `docs/IMPLEMENTATION_PLAN.md`, `docs/SECURITY_REVIEW.md`, `docs/TEST_MATRIX.md`, `docs/DEPLOYMENT_CHECKLIST.md`, `docs/PRIVACY_DATA_MAP.md` | Documentation review | None. |
| Batch 1 / Phase 2 | Converter state machine and error taxonomy | Implemented | `src/lib/converterState.js`, `index.js` | `tests/converter-state.test.js` | None. |
| Batch 1 / Phase 2 | Persistent job state across refresh/restart | Partial | `index.js`, `src/lib/converterState.js` | `tests/converter-state.test.js` | In-memory processing cannot resume an already-running yt-dlp child after process death; active jobs recover as retryable failed. Full durable worker resume requires Redis/database job backend. |
| Batch 1 / Phase 2 | Cancel, retry, idempotency, duplicate-click protection | Partial | `index.js` | `npm test`, route smoke | Queued jobs cancel fully; active job cancellation is cooperative and takes effect on progress/return because current converter code does not expose child-process handles to the queue. |
| Batch 1 / Phase 2 | Queue limit, timeout, cleanup, expiry, orphan cleanup | Partial | `index.js`, `src/lib/env.js` | `npm test`, `npm run build` | Existing process timeout remains inside converter functions; deeper orphan/partial cleanup across all temp dirs needs broader converter refactor. |
| Batch 1 / Phase 2 | Graceful shutdown and worker crash recovery | Partial | `index.js`, `src/lib/converterState.js` | `node --check index.js`, route smoke | Process restart recovers active jobs as retryable failed; true resume requires external persistent queue. |
| Batch 2 / Phase 3 | Socket.IO authentication and per-event authorization | Partial | `index.js`, `public-ui/admin-dashboard.html` | `npm test`, route/CSRF smoke | Admin dashboard socket streams now require a verified admin cookie + CSRF token and admin events are emitted only to the admin room; remaining work is browser-level Socket.IO E2E for all public forum/voice flows. |
| Batch 2 / Phase 3 | CSRF for every admin state-changing action | Implemented | `index.js`, `public-ui/admin-dashboard.html`, `public-ui/admin-tickets.html`, `public-ui/admin-cookies.html`, `public-ui/admin-login.html`, `public-ui/index.html` | `npm test`, local CSRF smoke | None. |
| Batch 2 / Phase 3 | Deep SSRF DNS/redirect revalidation | Partial | `src/lib/urlSafety.js`, `index.js` | `tests/url-safety.test.js` | DNS resolution/private IP checks are implemented for converter/fetch entrypoints; redirect-chain revalidation inside yt-dlp still requires a controlled fetch/extractor wrapper or provider-side metadata proxy. |
| Batch 2 / Phase 3 | Upload magic bytes/re-encode/EXIF stripping | Partial | `src/lib/uploadSecurity.js`, `index.js` | `tests/upload-security.test.js` | Magic-byte, MIME spoofing, dimensions and size checks are implemented; true local re-encoding/EXIF stripping would require adding an image processing dependency, while Cloudinary delivery transformation is used for uploaded forum images. |
| Batch 3 / Phase 1/6/7 | Homepage IA, refresh-safe route split, public admin/debug cleanup, mobile overflow guardrails | Partial | `public-ui/index.html`, `index.js`, `tests/frontend-ia.test.js` | `npm test`, route smoke for `/studio`, `/history`, `/assistant`, `/community`, `/support`, `/account`, `/rewards`, `/cookies`, `/data-request`, `/community-guidelines` | Repository-side route shell and focused-home behavior are implemented; deeper code splitting into separate bundles requires a larger frontend module refactor. |
| Batch 4 / Phase 4/13/14 | Legal/data-request, copyright, version, real status | Implemented | `public-ui/privacy.html`, `public-ui/terms.html`, `public-ui/copyright.html`, `public-ui/cookies.html`, `public-ui/data-request.html`, `public-ui/community-guidelines.html`, `public-ui/status.html`, `public-ui/changelog.html`, `index.js`, `tests/legal-status.test.js`, `tests/assistant-chat.test.js` | `npm test`, route smoke for legal/status/data-request pages and `/api/status` | Live provider account health can only show configured/not_configured locally without production credentials; repository-side safe checks are implemented. |
| Batch 5 / Phase 10/11/12/13 | AI models, community, voice, gamification isolation, PWA | Partial | `index.js`, `public-ui/sw.js`, `public-ui/offline.html`, `public-ui/index.html`, `tests/batch5-ai-community-pwa.test.js`, `tests/assistant-chat.test.js` | `npm test`, local API/route smoke for `/api/ai/models`, forum report/block, `/rewards`, `/assistant`, `/community`, `/offline.html`, `/sw.js` | Repository-side model discovery, moderation/report/block, voice consent/participant checks, PWA cache versioning, and rewards isolation are implemented. Live provider health and multi-browser/multi-device WebRTC verification still require configured credentials and browser/device infrastructure. |
| Batch 6 / Phase 15/16 | Unit/integration/E2E/accessibility/CI | Partial | `package.json`, `.github/workflows/ci.yml`, `scripts/lint.mjs`, `scripts/typecheck.mjs`, `scripts/secret_scan.mjs`, `tests/security-regression.test.js`, `tests/socket-security.test.js`, `tests/upload-security-regression.test.js`, `tests/e2e-smoke.test.js`, `tests/accessibility-static.test.js` | `npm run lint`, `npm run typecheck`, `npm test`, `npm run test:security`, `npm run test:e2e`, `npm run test:accessibility`, `npm run build`, `npm run audit`, `npm run secret-scan` | Repository-side CI and smoke/security/a11y tests are implemented. Full Playwright/axe browser matrix and real mobile/WebRTC device verification require browser automation dependencies and device infrastructure. |
| Phase 17 | Railway deployment validation | Partial | `docs/DEPLOYMENT_CHECKLIST.md`, `docs/RAILWAY_DEPLOYMENT.md`, `railway.toml` | Local route smoke | Production Railway verification requires deployment access. |
